const solution1 = require('@easy/maximum_depth_of_binary_tree_1');

describe('maximum depth of binary tree', () => {
    let cases = [{
        input: [3, 9, 20, null, null, 15, 7],
        output: 3
    }];

    beforeEach(() => {
        cases = [{
            input: [[3, 9, 20, null, null, 15, 7]],
            output: 3
        }];
    });

});