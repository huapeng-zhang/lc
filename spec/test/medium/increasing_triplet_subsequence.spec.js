const s1 = require('@medium/increasing_triplet_subsequence_1');

describe('increasing triplet subsequence', () => {
    let cases = null;

    beforeEach(() => {
        cases = [{
            input: [[1,2,3,4,5]],
            output: true,
        }, {
            input: [[5,4,3,2,1]],
            output: false
        }];
    });
});
