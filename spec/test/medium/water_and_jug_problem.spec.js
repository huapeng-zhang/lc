const s1 = require('@medium/water_and_jug_problem_1');

describe('water and jug problem', () => {
    let cases = null;

    beforeEach(() => {
        cases = [{
            input: [3, 4, 5],
            output: true
        }, {
            input: [2, 6, 5],
            output: false
        }, {
            input: [0, 0, 0],
            output: true
        }]
    });
});
